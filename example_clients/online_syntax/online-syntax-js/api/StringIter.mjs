// generated by diplomat-tool
import { StringWrapper } from "./StringWrapper.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const StringIter_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.StringIter_destroy(ptr);
});

export class StringIter {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("StringIter is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            StringIter_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    #iteratorNext() {

        const result = wasm.StringIter_next(this.ffiValue);

        try {
            return result === 0 ? null : new StringWrapper(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
        }
    }

    [Symbol.iterator]() {

        const result = wasm.StringIter_to_iterable(this.ffiValue);

        try {
            return new StringIter(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
        }
    }

    next(){
        const out = this.#iteratorNext();

        return {
            value: out,
            done: out === null,
        };
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}